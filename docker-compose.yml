services:
  app:
    build: .
    container_name: dogtracker-backend
    working_dir: /app
    env_file:
      - .env
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    command:
      - sh
      - -c
      - |
        uv run python -m scripts.validate_env &&
        uv run uvicorn src.api.main:api --host 0.0.0.0 --port 8000

  test:
    build: .
    container_name: dogtracker-backend-test
    working_dir: /app
    env_file:
      - .env
    volumes:
      - .:/app
    command:
      - sh
      - -c
      - |
        uv run python -m scripts.validate_env &&
        uv run pytest &&
        uv run dbt test